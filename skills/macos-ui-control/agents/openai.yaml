interface:
  display_name: "macOS UI Control"
  short_description: "Inspect and operate any macOS app through live UI state"
  default_prompt: "Use $macos-ui-control to inspect a macOS app and safely complete the requested UI task."
