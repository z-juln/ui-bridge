interface:
  display_name: "UI Bridge Control"
  short_description: "Inspect and safely operate apps through UI Bridge"
  default_prompt: "Use $ui-bridge-control to inspect a supported desktop app and safely complete the requested UI task."
