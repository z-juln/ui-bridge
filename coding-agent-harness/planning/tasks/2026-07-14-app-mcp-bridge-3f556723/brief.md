# 统一 App MCP Bridge 内部名称

## Task ID

`2026-07-14-app-mcp-bridge-3f556723`

## 一句话结果

App 的展示名、内部程序名、内部标识、本地目录、启动项、客户端配置和当前文档全部统一为 App MCP Bridge。

## 边界

- 范围内：Swift 包与目标、App 包、签名标识、运行目录、启动项、配置脚本、Skill、当前文档和安装迁移。
- 范围外：Git 历史提交内容、Windows 实现、正式 Apple 公证。
- 迁移影响：新的内部标识会让 macOS 要求重新授权辅助功能和录屏。

## 完成判断

- 当前工作树全文搜索不再出现旧内部名称。
- 安装包中的程序名和内部标识均为 `app-mcp-bridge` / `com.juln.app-mcp-bridge`。
- 旧启动项、旧本地状态目录和旧安装进程完成安全迁移或清理。
- Cursor、WorkBuddy 配置指向新程序，构建、自检、安装和错误路径通过。

## 当前下一步

先完成内部目标和安装迁移，再统一更新配置与文档。
